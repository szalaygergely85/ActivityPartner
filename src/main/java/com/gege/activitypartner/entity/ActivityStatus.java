package com.gege.activitypartner.entity;

public enum ActivityStatus {
    OPEN,       // Activity is open for registration
    FULL,       // All spots taken
    CANCELLED,  // Activity cancelled by creator
    COMPLETED   // Activity has finished
}
